/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["images.pexels.com", "i.dummyjson.com", 'fakestoreapi.com', 'jsonplaceholder.typicode.com', 'localhost:3000'
        ]
    }
}

module.exports = nextConfig
